using System.Collections.Generic;

namespace CSharpDiscovery.QuestBonus
{
    public class SecretSanta
    {
        //Modifie la méthode suivante pour réaliser le tirage au sort du Secret Santa.
        //En entrée, tu reçois une liste de participant. En sortie, tu retourneras un dictionnaire qui dira qui doit offrir un cadeau à qui.
        //Attention 1 : une personne ne peut s'offrir un cadeau à soit même
        //Attention 2 : pour une même collection de personnes en entrée, le tirage au sort ne doit pas sortir toujours la même sortie 
        public static Dictionary<string,string> SecretSantaDraw(HashSet<string> people)
        {
            return null;
        }
    }
}
